name: Laporan dan Warning Portofolio

on:
  #schedule:
  #  - cron: '0 0 * * *' # Sesuaikan jadwal cron Anda di sini
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      # 1. Main Bot
      - name: Run Main Bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python main.py

      # # 2. Bot Simulasi Trading (SMC + TECH) -> DINONAKTIFKAN
      # - name: Run Simulasi Trading
      #   env:
      #     TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
      #     TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      #   run: |
      #     python paper_trader.py

      # # 3. Commit & Push semua file JSON state paper trading -> DINONAKTIFKAN JUGA
      # - name: Save Paper Trading States
      #   run: |
      #     git config --local user.email "action@github.com"
      #     git config --local user.name "GitHub Action Bot"
      #     git add *.json || true
      #     git commit -m "Auto-update status paper trading [skip ci]" || true
      #     git push || true
